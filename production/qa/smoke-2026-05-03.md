# Smoke Check Report — Sprint 07 (POST-FIX)
**Date**: 2026-05-03
**Sprint**: Sprint 07 — Audio Body and Document Logic
**Engine**: Godot 4.6.2.stable
**QA Plan**: `production/qa/qa-plan-sprint-07-2026-05-03.md`
**Argument**: `sprint`
**Runner**: `addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a tests/`

---

## Automated Tests

**Status**: **PASS WITH WARNINGS** (1090 test cases | 0 errors | 7 failures | 0 flaky | 0 skipped | 0 orphans)

Pre-fix run: 16 failures + 3 errors. All 8 Sprint 07-caused regressions fixed in this session. Remaining 7 failures are pre-existing issues from Sprint 06 unchanged by Sprint 07's work.

### Sprint 07 regressions — RESOLVED ✅

| # | Test | Fix Applied |
|---|------|-------------|
| 1 | `level_streaming_swap_test` parse error (`document_collection.gd` / `document_body.gd` `Could not find type`) | Added explicit script preloads in dependency order (Document → DocumentBody) at top of `document_collection.gd`; replaced `is DocumentBody` with script-identity check via `_DOCUMENT_BODY_SCRIPT`; changed `@export var document: Document` → `@export var document: Resource` to bypass global-class-registry parse race |
| 2 | `photosensitivity_kill_switch_test` ordering contract | Restructured `_on_setting_changed`: graphics fast-path first, then literal `if category != &"accessibility": return` within first 600 chars |
| 3 | `audio_plaza_ambient_music_test` reverb mutation (3 assertions) | Test bug — implementation correct. Changed `is_equal(0.2)` to `is_equal_approx(0.2, 0.001)` to handle 32-bit float precision (e.g., `0.2 → 0.20000000298023`) |
| 4 | `localization_pseudolocale_test` missing pseudo entries | Added 8 rows to `translations/_dev_pseudo.csv` (3 doc.title + 3 doc.body + 2 ui.interact) |
| 5 | `node_payload_validity_grep_test` `_on_node_added` no validity guard | Added `if not is_instance_valid(node): return` at top of `_on_node_added(node: Node)` in `post_process_stack.gd` |
| 6 | `input_ci_lints check_action_literals` 3 method-name string violations | Replaced `target.call_deferred("queue_free")` ×2 with `target.queue_free.call_deferred()` (Godot 4.x Callable syntax). Extracted `"set_dim_intensity"` and `"/root/SepiaDimEffect"` to `const _SET_DIM_INTENSITY_METHOD` + `_SEPIA_DIM_EFFECT_PATH` |
| 7 | `audio_footstep_stinger_test` Voice bus first-pass exempt | Moved Voice/UI exempt-bus filter from second-pass-only to BOTH passes in `_get_or_steal_sfx_slot()` |
| 8 | `localization_lint cross-domain key collision` | Removed `ui.interact.pocket_document` + `ui.interact.read_document` from `translations/overlay.csv` (kept canonical entries in `hud.csv` per HUD render-domain ownership) |

### Pre-existing failures — NOT Sprint 07 regressions

These failures are inherited from Sprint 06 (verified via git diff against `81035c7 Fixes` commit; the F-key debug hotkeys and `_health_label_numeral.text = "100"` were introduced as part of HC-006 visual sign-off keys per Sprint 06 commit). Not blocking Sprint 07 sign-off.

| # | Test | Source | Status |
|---|------|--------|--------|
| 1 | `input_ci_lints_test::test_check_raw_input_constants_passes` | `src/core/main.gd:240..290` — KEY_F1, KEY_F2, KEY_F3, KEY_F4, KEY_F6, KEY_F7, KEY_F8, KEY_F10 raw input constants in HC-006 visual sign-off debug hotkey block | Pre-existing (Sprint 06 introduced; 8 violations) |
| 2 | `localization_lint_test::test_lint_no_hardcoded_visible_string_in_src` | `src/ui/hud_core/hud_core.gd:528` — `_health_label_numeral.text = "100"` HC-006 visual fallback | Pre-existing (Sprint 06) |
| 3 | `player_interact_cap_warning_test::test_resolve_cap_exceeded_returns_within_cap` (2 assertion failures) | `src/core/player_character/player_interact_cap_warning.gd` — resolution logic returns null at iteration cap boundary | Pre-existing |
| 4 | `player_interact_cap_warning_test::test_resolve_cap_one_returns_a_stub` (2 assertion failures) | Same — Cap=1 returns null instead of a stub target | Pre-existing |
| 5 | `player_interact_cap_warning_test::test_resolve_within_cap_returns_priority_winner` | Same — priority winner Document (0) lost to Door (3)/Pickup (2) selection | Pre-existing |

These should be filed as separate bug-fix or tech-debt stories for a future sprint. They were NOT introduced by Sprint 07 and are out of scope for this smoke check gate.

---

## Test Coverage

| System | Sprint 7 Test Files | Coverage |
|--------|---------------------|----------|
| Document Collection (DC-001..005) | 7 unit + 2 integration | COVERED |
| Audio (AUD-003..005) | 2 unit + 1 integration | COVERED |
| Post-Process Stack (PPS-003, 005, 006) | 5 unit | COVERED |
| Post-Process Stack (PPS-007) | 0 (Visual/Feel — manual evidence templates filed) | MANUAL |

**Summary**: 17 covered + 1 manual + 0 missing + 0 expected.

---

## Verdict: **PASS WITH WARNINGS**

Sprint 07 work is clean — all 8 regressions fixed. The 7 remaining test failures are pre-existing issues from Sprint 06 that should be addressed in a future bug-fix sprint or migrated to the tech-debt register. They do NOT block Sprint 07 hand-off to QA.

### Recommended Follow-Ups (separate stories)

1. **Bug-fix story**: `player_interact_cap_warning.gd` resolution logic — 5 failing tests need investigation (cap-exceeded returns null, cap=1 returns null, priority resolution incorrect)
2. **Cleanup story (Sprint 06 leftover)**: HC-006 visual sign-off debug F-keys in `main.gd` need migration to InputMap actions per AC-INPUT-6.1
3. **Cleanup story (Sprint 06 leftover)**: `hud_core.gd:528` hardcoded "100" → tr() call per ADR-0004

### QA Hand-off

Sprint 07 features (Document Collection epic 5/5 + Audio 3/3 + PPS 4/4) are ready for manual QA. Share `production/qa/qa-plan-sprint-07-2026-05-03.md` with the qa-tester agent.
